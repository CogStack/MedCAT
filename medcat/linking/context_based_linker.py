class Linker(object):
    def __init__(self, cdb, config):
        self.cdb = cdb
        self.config = config

    def __call__(doc):
        r'''
        '''

        # Loop through entities and link if good
